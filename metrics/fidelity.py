import rdkit
import os
import skpf


if __name__ == "__main__":
    # from skfp.fingerprints import AtomPairFingerprint
    # from qm9.rdkit_functions import smiles_to_mols, build_molecule, build_xae_molecule

    # # Example usage
    # smiles = "CCO"
    # mols = smiles_to_mols(smiles)
    # fingerprints = mols_to_fingerprints(mols)
    # print(f"Fingerprints for {smiles}: {fingerprints}")
    print("This is a sandbox for testing and debugging purposes.")